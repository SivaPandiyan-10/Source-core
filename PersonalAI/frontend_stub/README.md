Frontend stub
----------------

This is a minimal static frontend stub (not a full Angular app) that demonstrates interactions with the backend API (chat, upload, news). It's provided as a lightweight starting point; replace with a full Angular app later.

Quick start

```powershell
cd PersonalAI/frontend_stub
npm install
npm start
```

The stub serves `src/index.html` and simple JS that calls the backend endpoints.
